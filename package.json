{
	"name": "feline-monorepo",
	"type": "module",
	"version": "0.0.0",
	"scripts": {
		"format": "biome format --write",
		"lint": "biome check && biome format",
		"lint:fix": "biome check --write",
		"test": "vitest run",
		"test:coverage": "vitest run --coverage",
		"typecheck": "tsc --noEmit"
	},
	"private": true,
	"workspaces": ["packages/*"],
	"devDependencies": {
		"@biomejs/biome": "^2.3.13",
		"@types/node": "^25.0.10",
		"pkgroll": "^2.22.0",
		"typescript": "^5.9.3",
		"vite-tsconfig-paths": "^6.0.5",
		"vitest": "^4.0.18"
	}
}
